/**
 * constructor
 * @description Constructor del controlador
 * @param {Object} params
 */
(function constructor(args) {

	"use strict";

	var params = args;

	/**
	 * bindEvents
	 * @description Establecemos eventos de controlador
	 */
	(function bindEvents() {
		console.log("bindEvents");
		//$.on("internalControllerEvent", onInternalControllerEvent);
		//$.addEventListener($.button, "click", onButtonClick);
	})();

})($.args); 